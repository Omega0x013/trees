namespace trees
{
	static class Fires
	{
		// Entry point to fires code,
		/**
		 * REMEMBER
		 * 
		 * Our trees are firebroken in blocks of the nearest 200,#
		 * so only set fire to trees in blocks, rather than the whole forest
		 */
		public static void fires(ref uint[] trees) {
			// Josh, you're up
		}
	}
}