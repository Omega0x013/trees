# Wolves / Deer

- `void Month()` -- every month
- `void Year()` -- every year
- `void Check(int syrup)` -- every day
- `bool hasDeer()`

In main / day:

```c#
Animals animals = new Animals();
// --> Day
animals.Check()
```
